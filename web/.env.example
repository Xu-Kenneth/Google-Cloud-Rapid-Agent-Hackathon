# Frontend configuration (copy to web/.env.local for local dev).
# Base URL of the Bull vs Bear backend API.
VITE_API_BASE=http://localhost:8000
# Arize Phoenix UI URL (for the "open trace tree" link).
VITE_PHOENIX_URL=http://localhost:6006
